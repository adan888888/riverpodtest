name: riverpodtest
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0  # 如果需要实际网络请求
  cupertino_icons: ^1.0.8
  # 添加 riverpod 核心库
  flutter_riverpod: ^2.4.0  # 使用最新稳定版本
  # 可选：添加 hooks_riverpod 以支持 HookWidget
  hooks_riverpod: ^2.3.6    # 如果使用 hooks
  go_router: ^16.0.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  riverpod_annotation: ^2.4.0

    
dev_dependencies:
  flutter_test:
    sdk: flutter

  riverpod_generator: ^2.4.0
  flutter_lints: ^5.0.0
  build_runner: ^2.5.4
  freezed: ^3.1.0
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true